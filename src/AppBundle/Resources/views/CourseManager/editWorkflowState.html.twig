{% extends '::base.html.twig' %}

{% block title %}Edit workflow{% endblock %}

{% block body %}
    <h1>Modification de l'état</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Renommer" class="btn btn-primary">
    <a href="{{ path('course_manager.promotion.workflow.edit', { 'promotionId': promotionId }) }}"
       class="btn btn-secondary mr-3"><i class="fa fa-chevron-left" aria-hidden="true"></i> Retour</a>
    {{ form_end(form) }}
{% endblock %}