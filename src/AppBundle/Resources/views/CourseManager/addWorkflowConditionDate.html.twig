{% extends '::base.html.twig' %}

{% block title %}Ajout d'une condition{% endblock %}

{% block body %}
    <h1>Ajout d'une condition sur une date</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    
    <div class="form-group">
        <a href="{{ path('course_manager.promotion.workflow.transition.rename', { 'promotionId': promotionId, 'transitionId': transitionId }) }}"
           class="btn btn-secondary"><i class="fa fa-times" aria-hidden="true"></i> Annuler</a>
        {{ form_end(form) }}
        <input type="submit" value="Ajouter" class="btn btn-primary">
    </div>
{% endblock %}