{% extends 'base.html.twig' %}

{% block title %}Nouveau contact entreprise{% endblock %}

{% block body %}
    <h1>Nouveau contact entreprise</h1>
    <hr />

    <div class="form-group">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Valider" class="btn btn-success"/>
        {{ form_end(form) }}
    </div>

    <div class="form-group">
        <a href="{{ path('companycontact_index', {'courseId': course.id}) }}" class="btn btn-primary">
            <i class="fa fa-chevron-left" aria-hidden="true"></i>
            Retourner sur la liste
        </a>
    </div>
{% endblock %}
