{% extends 'base.html.twig' %}

{% block title %}Nouveau contact entreprise{% endblock %}

{% block body %}
    <h1>Nouveau contact entreprise</h1>
    <hr />

    <div class="form-group">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="form-group">
                <a href="{{ path('companycontact_index', {'courseId': course.id}) }}" class="btn btn-secondary">
                    Annuler
                </a>
                
                <button type="submit" class="btn btn-primary">
                    Valider
                </button>
            </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
