{% extends '::base.html.twig' %}

{% block title %}Après la formation - Suivis alternants{% endblock %}

{% block body %}
    <h1>Votre situation après votre formation</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Modifier" class="btn btn-primary">
    <a href="{{ path('student.home') }}"
       class="btn btn-secondary"><i class="fa fa-times" aria-hidden="true"></i> Annuler</a>
    {{ form_end(form) }}
{% endblock %}