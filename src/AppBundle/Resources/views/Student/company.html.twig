{% extends '::base.html.twig' %}

{% block title %}Modification de l'entreprise{% endblock %}

{% block body %}
    <h1>Entreprise d'accueil</h1>
    <hr>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <a href="{{ path('student.home') }}"
       class="btn btn-secondary"><i class="fa fa-times" aria-hidden="true"></i> Annuler</a>
    <input type="submit" class="btn btn-primary">
    {{ form_end(form) }}
{% endblock %}