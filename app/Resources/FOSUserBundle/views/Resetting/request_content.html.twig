{% trans_default_domain 'FOSUserBundle' %}

<div class="jumbotron">
    <h1 class="display-4">Mot de passe oublié ?</h1>
    <p class="lead">Veuillez saisir votre adresse e-mail. Un lien vous sera envoyé afin que vous puissiez réinitialiser
        votre mot de passe.</p>
    <hr class="my-4">
    <form action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="form-inline">
        <label for="username" class="mr-sm-2">Votre adresse e-mail</label>
        <input type="email" id="username" class="form-control mb-2 mr-sm-2 mb-sm-0" name="username"
               required="required"/>
        <input type="submit" class="btn btn-primary" value="Envoyer"/>
    </form>
    <div class="mt-5">
        <a href="{{ path('homepage') }}">Retour à l'acceuil</a>
    </div>
</div>
